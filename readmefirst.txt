font : Poppins
https://fonts.google.com/specimen/Poppins

hex : #ffe13e (yellow), #187cff (blue), #404040 (base text)

**// Koneksi Database //**
- folder root /koneksi/id19023295_jasadesain.sql
- folder root /koneksi/koneksi.php

**// build TailwindCSS //**
- npx tailwindcss -i ./input.css -o ./output.css --watch

**// direktori asset //**
- folder root /Admin/gmbr